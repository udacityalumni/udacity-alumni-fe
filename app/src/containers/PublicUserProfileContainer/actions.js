import * as types from './constants';

// publicUserProfiledefaultAction :: None -> {Action}
export const publicUserProfileDefaultAction = () => ({
  type: types.PUBLICUSERPROFILE_DEFAULT_ACTION,
});
