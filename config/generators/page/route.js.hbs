    {
      path: '{{ path }}',
      getComponent(location, callback) {
        require.ensure([], () => {
          const {{ properCase name }} = require(
            './pages/{{ properCase name }}'
        ).default;
          callback(null, {{ properCase name }});
        });
      },
    },
$1
